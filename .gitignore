# Godot 4 import cache and editor state - rebuilt by --import
.godot/
# Web export output
/build/
/export/

# Web export output
build/
version.txt

# Manual scans / PDF are copyrighted - never commit them.
# Keep only the ILLUSTRATIONS.md digest that belongs in the repo.
manual/*
!manual/ILLUSTRATIONS.md
